/*
* Maiah Pardo, mapardo
* 2020 Fall CSE 101 PA2
* BigIntegerTest.c
* Test module for the Big Integer ADT
*/

#include <stdio.h>


/*
 BigInteger A, B, C;
 char* s = "-29\0"; // S MUST HAVE A NULL TERMINATOR
 A = stringToBigInteger(s);
 
 B = stringToBigInteger("25");
 
 
 C = sum(A, B);
 
 printBigInteger(stdout, C);
 
 
 freeBigInteger(&A);
 freeBigInteger(&B);
 freeBigInteger(&C);
 return 0;
 */
